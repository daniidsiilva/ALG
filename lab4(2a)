//з використанням циклу з параметром
#include <stdio.h>
int main() {
  int sum = 0;
  for (int i = 1; i <= 100; i++) {
    if (i % 5 == 0) {
      sum += i;
    }
  }
  printf("Сума чисел, що ділиться на 5 у діапазоні (1; 100] є: %d\n", sum);
  return 0;
}
